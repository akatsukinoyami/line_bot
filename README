Юзербот написан на библиотеке Pyrogram и предназначен для формирования лент из каналов телеграм.

Поддерживается несколько лент.

Для работы необходимо:
sudo python3 -m pip install pyrogram

Чтобы сделать канал лентой:
Необходимо указать фразу в config.py

Чтобы удалить ленту:
Необходимо указать фразу в config.py

Чтобы добавить канал в БД:
переслать сообщение из канала

Чтобы удалить канал из БД:
"Delete channel" в ответ на пост в ленте

Чтобы посмотреть список каналов в ленте: 
"List channels"

Чтобы посмотреть список своих лент:
"List lines"

Для вызова справки:
"!help"

Для вкл/выкл ленты:
"Turn Line on/off"